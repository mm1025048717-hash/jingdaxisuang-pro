# 精打细算 Pro - 启动说明

## 方式一：带后端数据库（推荐）

1. 打开终端，进入项目目录：
   ```bash
   cd "c:\Users\陈宣任\Desktop\穷人军师"
   ```

2. 启动服务：
   ```bash
   npm start
   ```

3. 在浏览器打开：**http://localhost:3000**

数据会保存在 `server/data/app.json`，重启后数据仍在。

---

## 方式二：仅前端（无后端）

直接用浏览器打开 `index.html`，数据保存在浏览器 localStorage。无需安装任何依赖。

---

## 环境要求

- Node.js 14+（用于带后端的启动方式）
- 现代浏览器（Chrome、Edge、Safari 等）
