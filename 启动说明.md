# 精打细算 Pro - 启动说明

## 方式一：带后端数据库（推荐）

1. 打开终端，进入项目目录：
   ```bash
   cd "c:\Users\陈宣任\Desktop\穷人军师"
   ```

2. 启动服务：
   ```bash
   npm start
   ```

3. 在浏览器打开：**http://localhost:3000**

数据会保存在 `server/data/app.json`，重启后数据仍在。

**OpenClaw 自动启动**：使用方式一启动时，系统会自动尝试启动 OpenClaw 小龙虾网关（首次会通过 npx 下载）。启动后可点击「小龙虾执行」在应用内调用。若无需自动启动，可设置环境变量 `OPENCLAW_AUTO_START=0`。若网关启动失败，可手动运行：`npx openclaw@latest gateway --port 18789 --allow-unconfigured --token local-dev-123`

**手机上用**：电脑开 `npm start` 后，手机和电脑连同一 WiFi，手机浏览器访问 `http://电脑IP:3000`（如 192.168.1.100:3000）。OpenClaw 网关会自动用电脑 IP，内嵌面板可正常使用。

---

## 方式二：仅前端（无后端）

直接用浏览器打开 `index.html`，数据保存在浏览器 localStorage。无需安装任何依赖。

---

## 环境要求

- Node.js 14+（用于带后端的启动方式）
- 现代浏览器（Chrome、Edge、Safari 等）
